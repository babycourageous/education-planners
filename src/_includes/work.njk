<section id="work" class="section">
  <h1 class="section-heading">{{work.heading}}</h1>
  <div class="wrapper">
    {% blurb %}{{work.blurb | md | safe}}{% endblurb %}
    <div id="js-portfolio" class="c-portfolio col-span-4 flex flex-wrap mt-6 -mx-3 overflow-hidden md:col-span-12">
      {%- for project in work.projects | sort(false, true, 'school') -%}
        <div class="c-portfolio__item w-1/3 my-3 px-3 overflow-hidden md:w-1/6">
          <!-- project thumbnail link -->
          <div class="w-full h-100px">
            <a class="flex justify-center items-center w-full h-full" href="portfolio.html#{{project.school_id}}" aria-haspopup="dialog" data-component="dialog" data-target="{{project.school_id}}" data-labelledby="{{project.school_id}}-title"
            data-describedby="{{project.school_id}}-content">
              <img src="assets/images/schools/logos/{{project.logo}}" class="block max-h-100px mx-auto" alt="{{project.school}}">
            </a>
          </div>
          <!-- end project thumbnail link -->
          <!-- project detail -->
          {%- include "project.njk" -%}
          <!-- end project detail -->
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>